server:
  port: 8087

spring:
  datasource:
    url: jdbc:h2:mem:cakedb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    user:
      name: admin
      password: admin123
logging:
  level:
    org.springframework: INFO
